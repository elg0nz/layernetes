# Python
.venv/
__pycache__/
*.egg-info/
.pytest_cache/

# Helm dependency downloads (Chart.lock is committed; the tgz is not)
ll-infra/charts/

# Local cluster credentials — never commit these, any spelling
*.kubeconfig
*kubeconfig*
kubeconfig
.kube/

# Generated Helm secret values (admin password, runner token) — see hack/talos-bootstrap.sh
*.secret.yaml
hack/talos.env

# OS / tooling
.DS_Store
.claude/
