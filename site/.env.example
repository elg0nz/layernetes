# PostHog analytics (see src/components/posthog.astro).
# These are PUBLIC values — the project token is a write-only key that ships in
# the client bundle. Copy this file to .env and fill in for local dev; CI reads
# the same values from repo variables (Settings → Secrets and variables → Actions).
PUBLIC_POSTHOG_PROJECT_TOKEN=phc_u56e7Aa6naRgEuui4AqGWrSKDZwLKmsgzmAYsx3gbawf
PUBLIC_POSTHOG_HOST=https://us.i.posthog.com
